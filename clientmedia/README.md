Optional client media lives here.

- `music/` for background music and zone tracks
- `sfx/` for sound effects and ambient audio

The core client should treat these assets as optional and fail silently if a file is missing.
